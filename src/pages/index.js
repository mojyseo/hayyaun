import * as React from 'react';
import MainSection from '../sections/MainSection';

const IndexPage = () => {
  return (
    <main>
      <MainSection />
    </main>
  );
};

export default IndexPage;
